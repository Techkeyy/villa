# Names only. Never put real values in this file or in git.

# Somnia Shannon testnet
RPC_URL=
CHAIN_ID=
WS_RPC_URL=
INDEXER_URL=
PRICE_FEED_URL=

# Disposable Shannon development wallet only. Never a wallet that holds real value.
OPERATOR_PRIVATE_KEY=
OPERATOR_ADDRESS=

# Private operator API. The engine key stays on the owner VPS; Vercel only needs
# VILLA_ENGINE_API_URL after that service has an HTTPS endpoint.
VILLA_BIND_HOST=127.0.0.1
VILLA_ALLOWED_ORIGINS=
VILLA_ENGINE_API_URL=
VILLA_EXECUTION_ENABLED=false
VILLA_ACCOUNT_EXECUTION_ENABLED=false

# Optional second disposable wallet used only to generate disclosed demo fills.
TAKER_PRIVATE_KEY=
TAKER_ADDRESS=

# Venue scoping. Values move; read from a live market row if discovery returns none.
VENUE_ID=
NETWORK=
